// Re-export sonner's Toaster with micro-os theme applied.
// Usage: place <Toaster /> once in the app root.
// Call toast(), toast.success(), toast.error() etc. from 'sonner' directly.
export { Toaster } from 'sonner'
export { toast } from 'sonner'
